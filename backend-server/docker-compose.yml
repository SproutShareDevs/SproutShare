version: '3'
services:
  backend:
    container_name: node-docker-test
    restart: always
    build: .
    ports:
      - '3000:3000'
    links:
      - mongo
  mongo:
    container_name: mongo
    image: mongo
    volumes:
      - ./data:/data/db
    ports:
      - '27017:27017'
  #frontend:
   # container_name: frontend-docker-test
    #restart: always
    #build: ../sproutshare-client
    #ports:
    #  - '19001:19000'
    #links:
    #  - mongo
    #  - backend